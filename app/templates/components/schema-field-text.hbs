<div class="form-group">
  {{#if property.title}}
    <label for="{{key}}">{{property.title}}</label>
  {{/if}}

  {{input type="text"
    name=key
    value=value
    autofocus=true
    class="form-control"}}
</div>
